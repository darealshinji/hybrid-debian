
log="disabled"
update="enabled"
for opt; do
    case "$opt" in
        -log)
            log="enabled"
            ;;
        -no-update)
            update="disabled"
            ;;
        *)
            ;;
    esac
done

. "$prefix/share/hybrid-common/run-hybrid"
